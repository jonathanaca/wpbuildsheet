import React from 'react';
import { PlaceholderForm } from './PlaceholderForm';

export const VisitorKioskInductionForm: React.FC = () => {
  return (
    <PlaceholderForm
      title="Visitor Kiosk Induction"
      description="Configure custom induction wording displayed on visitor kiosks during the check-in process."
    />
  );
};
