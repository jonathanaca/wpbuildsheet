import React from 'react';
import { PlaceholderForm } from './PlaceholderForm';

export const LockersForm: React.FC = () => {
  return (
    <PlaceholderForm
      title="Lockers"
      description="Configure locker booking settings and determine if lockers are booked separately or with desk bookings."
    />
  );
};
