import React from 'react';
import { PlaceholderForm } from './PlaceholderForm';

export const RoomsForm: React.FC = () => {
  return (
    <PlaceholderForm
      title="Rooms"
      description="Configure meeting rooms, phone booths, and focus rooms. Set capacity, features, booking rules, and autorelease settings."
    />
  );
};
