import React from 'react';
import { PlaceholderForm } from './PlaceholderForm';

export const DesksForm: React.FC = () => {
  return (
    <PlaceholderForm
      title="Desks"
      description="Configure desk booking settings including autorelease, user groups, features, and approval methods."
    />
  );
};
