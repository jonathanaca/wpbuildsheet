import React from 'react';
import { PlaceholderForm } from './PlaceholderForm';

export const NavigationLinksForm: React.FC = () => {
  return (
    <PlaceholderForm
      title="Navigation Links"
      description="Configure custom navigation links for the PlaceOS interface. Update template text and links."
    />
  );
};
