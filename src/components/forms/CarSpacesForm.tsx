import React from 'react';
import { PlaceholderForm } from './PlaceholderForm';

export const CarSpacesForm: React.FC = () => {
  return (
    <PlaceholderForm
      title="Car Spaces"
      description="Configure car and bike parking spaces. Set up number plate recognition, EV charging, and booking rules."
    />
  );
};
