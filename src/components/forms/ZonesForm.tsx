import React from 'react';
import { PlaceholderForm } from './PlaceholderForm';

export const ZonesForm: React.FC = () => {
  return (
    <PlaceholderForm
      title="Zones"
      description="Configure zones for each building and level. Define capacity, user groups, people counting and finding requirements."
    />
  );
};
