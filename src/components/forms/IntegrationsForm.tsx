import React from 'react';
import { PlaceholderForm } from './PlaceholderForm';

export const IntegrationsForm: React.FC = () => {
  return (
    <PlaceholderForm
      title="Integrations"
      description="Manage required integrations such as Azure App Registration, OAuth, Cisco Meraki, DNA Spaces, and Outlook Plugin."
    />
  );
};
